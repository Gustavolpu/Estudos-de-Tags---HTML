# 📘 Estudos de HTML

Este repositório foi criado para armazenar meus **estudos iniciais de HTML**, utilizando o **Notepad++** como editor de código.

O objetivo é praticar a estrutura básica do HTML e testar as principais **tags textuais, semânticas e de formatação**.

---

## 🧠 Conteúdos praticados

### 🔹 Estrutura básica do HTML
- `<!DOCTYPE html>`
- `<html>`
- `<head>`
- `<meta charset="utf-8">`
- `<title>`
- `<body>`

---

### 🔹 Tags de texto
- `<h1>` e `<h2>` (cabeçalhos)
- `<p>` (parágrafo)
- `<strong>` (texto importante)
- `<em>` (ênfase)
- `<s>` (texto tachado)
- `<sub>` (subscrito)

---

### 🔹 Tags de formatação
- `<i>` (itálico)
- `<del>` (texto excluído)
- `<ins>` (texto inserido)

---

### 🔹 Tags semânticas
- `<nav>` (navegação)
- `<a>` (links)
- `<img>` (imagens com atributo `alt`)

---


## 🖥️ Editor utilizado
- **Notepad++**

> O GitHub foi utilizado apenas para versionamento e organização dos estudos.

---

## 🎯 Objetivo do repositório
- Fixar conceitos básicos de HTML  
- Criar histórico de aprendizado  
- Servir como base para estudos futuros de **CSS e JavaScript**
